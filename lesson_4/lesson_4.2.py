list_1 = [0, 1, 7, 2, 4, 8]

if list_1[-1] != 0:    #Проводимо перевірку на останній елемент списку, якщо 0 - беремо значення результату за замовчуванням
    result = sum(list_1[::2]) * list_1[-1]    #обчислюємо результат
else:
    result = 0

#Виводимо результат обчислення
print(result)
